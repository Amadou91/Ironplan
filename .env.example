# Supabase (required for app runtime/build)
NEXT_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Server-only Supabase keys (optional; only if you add server-side admin tasks)
# Never expose these to the client or prefix with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Only required if you verify Supabase JWTs manually outside the Supabase SDK.
SUPABASE_JWT_SECRET="your-jwt-secret"
